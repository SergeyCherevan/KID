<Window x:Class="KID.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:avalonedit="http://icsharpcode.net/sharpdevelop/avalonedit"
        xmlns:views="clr-namespace:KID.Views"
        xmlns:viewModels="clr-namespace:KID.ViewModels"
        Title=".KID - C#/.NET IDE для детей"
        Height="600" Width="800"
        WindowState="{Binding WindowState, Mode=TwoWay}"
        WindowStyle="None"
        AllowsTransparency="False" 
        ResizeMode="CanResize"
        Icon="Resources/KID.ico"
        Background="{StaticResource SpecialElementsBrush}">

    <Window.DataContext>
        <viewModels:MainViewModel />
    </Window.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="32"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <Border Grid.Row="0"
                Background="{StaticResource WindowBrush}"
                Height="32">
            <DockPanel LastChildFill="True" MouseDown="TitleBar_MouseDown">
                <Image Source="Resources/KID.ico"
                       Width="30" Height="30"
                       Margin="7.5,0,0,0"
                       VerticalAlignment="Center" />
                <StackPanel Orientation="Horizontal"
                            DockPanel.Dock="Right"
                            VerticalAlignment="Center">
                    <Button Name="MinimizeButton" 
                            Style="{StaticResource WindowButtonStyle}" 
                            Command="{Binding MinimizeCommand}" 
                            Content="—"/>
                    <Button Name="MaximizeButton" 
                            Style="{StaticResource WindowButtonStyle}" 
                            Command="{Binding MaximizeCommand}" 
                            Content="{Binding MaximizeButtonContent}"/>
                    <Button Name="CloseButton" 
                            Style="{StaticResource WindowButtonStyle}" 
                            Click="Close_Click" 
                            Content="╳"/>
                </StackPanel>
                <TextBlock Text=".KID — C#/.NET IDE для детей"
                           DockPanel.Dock="Left"
                           Foreground="{StaticResource SpecialElementsBrush}"
                           VerticalAlignment="Center"
                           FontWeight="Bold"
                           FontSize="13"
                           Margin="7.5,0,0,0"/>
            </DockPanel>
        </Border>

        <!-- Меню -->
        <views:MenuView x:Name="MenuView" Grid.Row="1" />

        <!-- Рабочая зона -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="5" />
                <ColumnDefinition Width="310" MinWidth="310" />
            </Grid.ColumnDefinitions>

            <!-- Код -->
            <views:CodeEditorView x:Name="CodeEditorView" Grid.Column="0" Margin="5" />

            <!-- Разделитель -->
            <GridSplitter Grid.Column="1" Width="5"
                          HorizontalAlignment="Center"
                          VerticalAlignment="Stretch"
                          Background="{StaticResource SplitterBrush}" />

            <!-- Вывод -->
            <Grid Grid.Column="2" Margin="5">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="5" />
                    <RowDefinition MinHeight="300" />
                </Grid.RowDefinitions>

                <!-- Консоль -->
                <views:ConsoleOutputView x:Name="ConsoleOutputView" Grid.Row="0" />

                <!-- Вертикальный разделитель -->
                <GridSplitter Grid.Row="1"
                              Height="5"
                              HorizontalAlignment="Stretch"
                              VerticalAlignment="Center"
                              Background="{StaticResource SplitterBrush}" />

                <!-- Графика -->
                <views:GraphicsOutputView x:Name="GraphicsOutputView" Grid.Row="2" />
            </Grid>
        </Grid>
    </Grid>
</Window>
