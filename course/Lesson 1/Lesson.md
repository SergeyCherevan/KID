# Урок 1. Привет, C#! Привет, .KID!

Представь, что компьютер — это очень быстрый, очень послушный робот.
Он может рисовать, писать слова, считать, играть звуки… но есть одна хитрость: **робот понимает только команды**.

Программирование — это искусство и ремесло одновременно: мы учимся **говорить с роботом так, чтобы он делал то, что мы задумали**.
Сегодня ты впервые скажешь компьютеру: «Привет!» — и он ответит тебе рисунком.

---

## 1. Введение в C# и .KID

**C# (произносится “си-шарп”)** — настоящий “взрослый” язык программирования.
На нём пишут игры, приложения, сайты и даже программы для роботов.

А **.KID** — это особая среда, которая помогает начать дружелюбно:
- слева у тебя будет **код** (команды),
- справа — **результат**: слова в консоли и рисунки на холсте.

Сегодня мы сделаем программу, которая:
- напишет в консоли *Hello World!*,
- нарисует круг и прямоугольник,
- и покажет надпись на рисунке.

---

## 1.1. Знакомство со средой .KID IDE

Открой .KID. Перед тобой главное окно.

### Что где находится

- **Слева** — редактор кода. Здесь мы пишем программу.
- **Справа сверху** — **консоль**: туда программа выводит текст.
- **Справа снизу** — **поле для рисунка** (его часто называют “холст” или Canvas): туда программа рисует фигуры и текст.
- Между областями есть тонкие “перемычки-разделители” — их можно тянуть мышкой и менять размеры панелей.

### Как запустить программу

Ищи в меню кнопку **▶️ Запустить**.
- Горячая клавиша запуска: **Ctrl+F5**.
- Остановка программы: **Shift+F5**.

Важно: перед каждым запуском .KID **очищает консоль и холст**, чтобы ты видел результат именно текущего запуска.

### Иллюстрация 1 — интерфейс .KID

Здесь будет картинка, где стрелочками подписано: “Код”, “Консоль”, “Рисунок”.

- Путь для будущего изображения: `Images/01_kid-ide-overview.png`
- Промт для генерации иллюстрации (если нужно вместо реального скриншота):

> **PROMPT (RU)**: Иллюстрация для детского учебника программирования. Окно простой IDE “.KID”: слева светлая область редактора кода с C# (несколько строк), справа сверху консоль с одной строкой вывода, справа снизу серый холст (canvas). Между областями видны тонкие разделители (splitters). Стиль: чистый, современный, дружелюбный, без лишних деталей, мягкие цвета. Без мелкого текста, только крупные подписи-стрелки: «Код», «Консоль», «Рисунок».

---

## 1.2. Первая программа: Hello World с графикой — что она делает?

Сейчас будет твоя первая программа. Не бойся, если пока непонятно каждое слово.
Сначала мы **запустим** и посмотрим на результат, а потом разберёмся, как всё устроено.

Открой файл со скриптом урока:
- `Scripts/01_HelloWorld_Graphics.cs`

Или просто набери/вставь этот код в редактор:

```csharp
using System;
using KID;

Console.WriteLine("Hello World!");

Graphics.Color = "Red";
Graphics.Circle(150, 150, 125);

Graphics.Color = "Blue";
Graphics.Rectangle(150, 150, 100, 100);

Graphics.Color = "White";
Graphics.SetFont("Arial", 25);
Graphics.Text(150, 150, "Hello\nWorld!");
```

Теперь нажми **▶️ Запустить** (или **Ctrl+F5**).

### Что ты увидишь

1) В консоли появится строка:
- `Hello World!`

2) На сером поле рисунка появятся:
- **красный круг** (центр примерно посередине),
- **синий прямоугольник**,
- и **белая надпись** “Hello” и “World!” на двух строках.

### Иллюстрация 2 — результат выполнения

- Путь для будущего изображения: `Images/02_hello-world-result.png`
- Промт для генерации иллюстрации:

> **PROMPT (RU)**: Кадр из детского учебника программирования: серый холст (canvas) на фоне. На холсте красный круг (залитый) с центром примерно в точке (150,150) и большим радиусом. Поверх/рядом с ним синий прямоугольник (залитый) с левым верхним углом около (150,150), размер примерно 100x100. Белым цветом поверх фигур надпись “Hello” на первой строке и “World!” на второй строке. Стиль: чистый, плоский, без 3D, без лишних объектов.

---

## 1.3. Разбираем код: консоль и графика

Теперь — самое интересное. Мы превращаем “магическое заклинание” в понятные кирпичики.

### 1) `Console.Write()` и `Console.WriteLine()` — как программа говорит словами

`Console` — это “говорилка” для текста.

- `Console.Write("текст")` — печатает текст **без перехода на новую строку**.
- `Console.WriteLine("текст")` — печатает текст **и сразу переходит на новую строку**.

Это как в тетради:
- `Write` — пишем и **не нажимаем Enter**.
- `WriteLine` — пишем и **нажимаем Enter**.

Посмотри мини-пример (он лежит в `Scripts/02_Write_vs_WriteLine.cs`):

```csharp
using System;

Console.Write("Счёт: ");
Console.Write(1);
Console.Write(", ");
Console.Write(2);
Console.WriteLine(" — готово!");

Console.WriteLine("Эта строка началась уже с новой строки.");
```

Попробуй запустить этот пример и увидишь, как строки “склеиваются” или “переносятся”.

### 2) Простые команды графики: круг, прямоугольник, текст

В .KID есть класс `Graphics`. Он умеет рисовать.

Мы сегодня используем три команды:

- `Graphics.Circle(x, y, radius)` — рисует круг.
  - **`x, y` — это центр круга**.
  - `radius` — радиус (насколько круг “большой”).

- `Graphics.Rectangle(x, y, width, height)` — рисует прямоугольник.
  - **`x, y` — это левый верхний угол**.
  - `width, height` — ширина и высота.

- `Graphics.Text(x, y, text)` — пишет текст на холсте.
  - **`x, y` — точка, где начинается надпись (слева сверху)**.

И ещё важная деталь про координаты:
- точка `(0, 0)` находится **в левом верхнем углу холста**,
- `x` растёт вправо, `y` растёт вниз.

### 3) Как задавать цвет фигурам

Перед тем как рисовать, мы пишем:

- `Graphics.Color = "Red";`

Это значит: “Следующие фигуры рисуй красным”.

В этом уроке мы будем пользоваться **самым простым способом** — названием цвета строкой:
- `"Red"`, `"Blue"`, `"White"`.

Позже ты узнаешь, что цвета можно задавать ещё и по-другому (например, специальными кодами вроде `#FF0000`),
но пока нам важнее научиться думать как программист: **сначала настроили инструмент, потом рисуем**.

### 4) Зачем нужны `using` — как подключать “наборы инструментов”

В начале программы есть строки:

- `using System;`
- `using KID;`

Представь, что `using` — это как табличка на двери: “В этой комнате лежат нужные инструменты”.

- `System` — это “мир C#”: там живёт, например, `Console`.
- `KID` — это “мир .KID”: там живут `Graphics`, `Music`, `Mouse` и другие.

А что будет, если **не писать** `using KID;`?
Тогда тебе придётся каждый раз писать полное имя инструмента, как “имя + фамилия”:

Смотри файл `Scripts/01_HelloWorld_Graphics_NoUsingKID.cs`:

```csharp
using System;

Console.WriteLine("Hello World!");

KID.Graphics.Color = "Red";
KID.Graphics.Circle(150, 150, 125);

KID.Graphics.Color = "Blue";
KID.Graphics.Rectangle(150, 150, 100, 100);

KID.Graphics.Color = "White";
KID.Graphics.SetFont("Arial", 25);
KID.Graphics.Text(150, 150, "Hello\nWorld!");
```

Вроде бы работает, но выглядит длиннее и “тяжелее”. Поэтому `using` — это наш способ писать **короче и понятнее**.

---

## Мини-задания (на 10 минут)

1) **Сменить цвета**
- Сделай круг зелёным (`"Green"`), а прямоугольник — жёлтым (`"Yellow"`).

2) **Передвинуть рисунок**
- Увеличь `x` у круга на 50. Что произошло?
- Увеличь `y` у прямоугольника на 50. Что произошло?

3) **Изменить надпись**
- Напиши вместо “Hello World!” своё имя.
- Сделай шрифт больше или меньше, меняя число в `Graphics.SetFont("Arial", 25);`.

Если получилось — поздравляю. Ты уже умеешь то, с чего начинается любая программа: **давать компьютеру команды и проверять результат**.

В следующем уроке мы научим программу **задавать вопросы** (ввод с клавиатуры) и делать первые маленькие игры.
