# .KID - C#/.NET IDE для детей

## Обзор проекта

**.KID** — это образовательная интегрированная среда разработки (IDE) для обучения детей программированию на языке C#. Проект разработан специально для начинающих программистов и предоставляет упрощённый интерфейс с визуальным выводом графики и консольным выводом.

### Основная идея

.KID создан для того, чтобы сделать обучение программированию доступным и увлекательным для детей. IDE предоставляет:

- **Простой редактор кода** с подсветкой синтаксиса
- **Визуальный вывод графики** через упрощённый API
- **Консольный ввод/вывод** для текстовых программ
- **Многоязычный интерфейс** (русский, английский, украинский)
- **Темы оформления** (светлая и тёмная)

### Целевая аудитория

- Дети и подростки, начинающие изучать программирование
- Преподаватели информатики
- Родители, желающие обучить детей программированию

## Основные возможности

### 1. Редактор кода
- Редактор на базе AvalonEdit с подсветкой синтаксиса C#
- Панель вкладок — несколько открытых файлов, переключение, закрытие
- Индикация активной вкладки и несохранённых изменений
- Поддержка отмены/повтора действий (Undo/Redo)
- Автоматическое форматирование

### 2. Выполнение кода
- Компиляция C# кода в реальном времени
- Выполнение скомпилированного кода
- Обработка ошибок компиляции и выполнения
- Возможность остановки выполнения программы

### 3. Воспроизведение звуков
- API для воспроизведения тонов и музыки
- Структура `SoundNote` для представления звуков
- Полифоническое воспроизведение (несколько дорожек одновременно)
- Проигрывание аудиофайлов (WAV, MP3 и др.)
- Расширенное управление воспроизведением (пауза, остановка, зацикливание)

### 4. Графический вывод
- Упрощённый API для рисования фигур (круги, прямоугольники, линии, многоугольники)
- Работа с цветами в различных форматах
- Вывод текста на графическом холсте
- Кривые Безье (квадратичные и кубические)

### 5. Информация от мыши
- API для получения координат курсора и кликов относительно Canvas
- Поддержка одинарных и двойных кликов
- События перемещения/кликов/изменения нажатых кнопок

### 6. Консольный ввод/вывод
- Перенаправление `Console.WriteLine` и `Console.Write` в текстовое поле
- Поддержка `Console.ReadLine` для ввода данных
- Очистка консоли

### 7. Интерфейс
- Многоязычная поддержка (ru-RU, en-US, uk-UA)
- Темы оформления (Light, Dark)
- Настраиваемое окно без стандартных рамок Windows
- Разделяемые панели для удобной работы

### 8. Работа с файлами
- Создание нового файла
- Открытие существующих .cs файлов (несколько вкладок)
- Сохранение и «Сохранить как»
- «Назначить шаблоном по умолчанию (и сохранить)»
- Отслеживание несохранённых изменений

## Технологический стек

- **.NET 8.0** — платформа разработки
- **WPF** — пользовательский интерфейс
- **AvalonEdit** — редактор кода с подсветкой синтаксиса
- **Microsoft.CodeAnalysis** — компиляция C# кода
- **Microsoft.Extensions.DependencyInjection** — внедрение зависимостей
- **NAudio** — библиотека для работы со звуком

## Структура проекта

```
KID/
├── KID.WPF.IDE/         # Основное WPF-приложение (UI + MVVM + сервисы)
│   ├── Models/          # Модели данных
│   ├── Services/        # Сервисы приложения
│   ├── ViewModels/      # ViewModels (MVVM)
│   ├── Views/           # Представления (XAML)
│   ├── Resources/       # Ресурсы (строки, иконки)
│   └── Themes/          # Файлы тем оформления
└── KID.Library/         # Библиотека для пользовательского кода (API)
    ├── Graphics/        # API для графики
    ├── Sprite/          # Спрайты (объекты из нескольких элементов, столкновения)
    ├── Music/           # API для воспроизведения звуков
    ├── Mouse/           # API для мыши
    └── Keyboard/        # API для клавиатуры
```

## Быстрый старт

1. Откройте проект в Visual Studio
2. Соберите решение (Build Solution)
3. Запустите приложение (F5)
4. Напишите код в редакторе (вкладка NewFile по умолчанию)
5. Нажмите "▶️ Запустить" (Ctrl+F5) для выполнения

### Пример кода

```csharp
using System;
using KID;

// Приветствие в консоли
Console.WriteLine("Привет, Мир!");

// Рисуем круг
Graphics.Color = "Blue";
Graphics.Circle(150, 150, 50);

// Воспроизводим мелодию
Music.Sound(
    new SoundNote(262, 500),  // До
    new SoundNote(294, 500),  // Ре
    new SoundNote(330, 500)   // Ми
);

// Отслеживаем позицию мыши
var cursor = Mouse.CurrentCursor;
if (cursor.Position.HasValue)
{
    Console.WriteLine($"Мышь на позиции: {cursor.Position.Value.X}, {cursor.Position.Value.Y}");
}
```

## Документация

- [Архитектура проекта](ARCHITECTURE.md) — общее описание архитектуры и компонентов
- [Подсистемы проекта](SUBSYSTEMS.md) — детальное описание всех подсистем и их взаимодействия
- [Функциональность](FEATURES.md) — подробное описание всех функций
- [Console API](Console-API.md) - полная документация по Console API
- [Graphics API](Graphics-API.md) — полная документация по Graphics API
- [Sprite API](Sprite-API.md) — документация по спрайтам и столкновениям
- [Music API](Music-API.md) — полная документация по Music API
- [Mouse API](Mouse-API.md) — полная документация по Mouse API
- [Keyboard API](Keyboard-API.md) — полная документация по Keyboard API
- [Руководство разработчика](DEVELOPMENT.md) — информация для разработчиков

## Лицензия

Проект разработан для образовательных целей.

## Контакты

Для вопросов и предложений создавайте Issues в репозитории проекта.

